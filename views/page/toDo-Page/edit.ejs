<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Edit Todo</title>
  <!-- Include Tailwind CSS -->
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-900 min-h-screen flex items-center justify-center">
  
  <!-- Edit Todo Form Container -->
  <div class="bg-gradient-to-b from-gray-800 to-gray-900 p-8 rounded-2xl shadow-2xl text-white w-full max-w-lg">
    <h2 class="text-3xl font-bold mb-6 text-center">Edit Todo</h2>
    
    <!-- Form for Editing Todo -->
    <form action="/user/editTodo/<%= data._id %>?_method=PATCH" method="post" class="space-y-6">
      
      <!-- Input for Task -->
      <div>
        <label for="task" class="block text-lg font-medium mb-2">Task:</label>
        <input type="text" id="task" name="task" value="<%= data.task %>"
               class="w-full px-4 py-2 border border-gray-600 rounded-lg bg-gray-700 text-gray-300 focus:outline-none focus:ring-4 focus:ring-blue-500 transform transition-transform duration-300 focus:scale-105">
      </div>
      
      <!-- Edit Button -->
      <div class="text-center">
        <button type="submit"
                class="bg-blue-600 text-white py-3 px-8 rounded-lg shadow-lg hover:bg-blue-700 transform hover:-translate-y-1 transition-all duration-300 hover:shadow-xl">
          Save Changes
        </button>
      </div>
    </form>

    <!-- Back Link -->
    <div class="mt-8 text-center">
      <a href="/user/getTodos"
         class="inline-block bg-gray-700 hover:bg-gray-600 text-white font-medium py-2 px-6 rounded-lg shadow-md focus:outline-none transition-all duration-300 transform hover:scale-105">
        Back to Todos
      </a>
    </div>
  </div>
</body>
</html>
